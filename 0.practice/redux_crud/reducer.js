const reducer = (state, action) => {
    // state 초기값
    if (state === undefined) {
        return {
            titles: [],
            article: {
                
            }
        }
    }

    let newState;

    return newState;
}

export {reducer}